*********** Property *****************

------------ Selector in CSS -------------------------
        Simple Selector -: Name , Id , Class
        Group  Selector  -: all selector used with the help of comma (,) ex:- h1 , p , div { styling }
        Universal selector -: " * " this is universal selector for all tags take style.
        Attribute selector -: select by the Attributeof the element
        Pseudo class -: 
        Pseudo Element -: syntax selector::Property{ Property } Example(p::after{ content: '💋💖' ; })
        Combinator selector -: descendant selector ( > ), child selector ( space b/w two element), general sibling selector , adjacent sibling selector



Margin - Space  between two elements.
Padding - Space inside elements (space between content & Border of the element.)
Box-sizing :-  it is used for fixing the width & height of the elements.
Text-align :- For alignment of content in block element.
 
----------- Position Property -------------
 1. Static :- Fixed the element on the Position.
 2. Relative :- To move from the original Position.
 3. Absolute :- to stick with the parent top.
 4. Sticky :-
 5. Fixed :-

(interview Question ) Note :- diff b/w Absolute & Relative ?
In Relative the element move from the previous original Position and in Absolute first it stcik with our parent then move.

---------- Display Property ---------------
Inline :- its Property used for a to convert a block element into inline element.
Block :-its Property used for a to convert a inline element into Block element.
None :- To hide the element and content.
Visibility Hidden :- To hide the element and content.
Flex :- its help to provide flexibility in element.


(interview Question ) Note :- diff b/w None & Hidden item ?
In none its hide & remove our space and vacant the space but in Visibility hidden it remove but not remove our space & cover it.

---------- Justify-content ----------------
Imp :- For a horizontal
Space-between 
space-around 
centre
space-evenly

--------- Align-items ------------------
Imp :- For a Vertical
centre

flex-direction
flex-wrap
flex-grow
flex-shrink
flex-flow is the shortened Property of flex-direction &  flex-wrap.
Order Property :- for a give a sequence.

----------- Transition ---------------
used for smoothly shift between two Property
Transition:width 2s,(Property timing).

